<script charset="utf-8">
  var chart = c3.generate({
    bindto: '#chart3',
    data: {
      columns: [
        ['Novo Curso Terminado', 30, 200, 100, 400, 150, 250],
        ['Novo Curso Iniciado', 130, 100, 140, 200, 150, 50]
      ],
      type: 'bar'
    },
    bar: {
      width: {
        ratio: 0.5 // this makes bar width 50% of length between ticks
      }
      // or
      //width: 100 // this makes bar width 100px
    }
  });

setTimeout(function () {
  chart.load({
    columns: [
      ['Matriculas Ainda não Iniciadas', 130, 120, 200, 300, 85, 100]
    ]
  });
}, 1000);

</script>
